{% extends "base.html.twig" %}
{% block title %}{{ title }}{% endblock %}
{% block header %}Bibliotek{% endblock %}

{% block body %}
    <h2>{{ title }}</h2>
    <form method="POST" action="{{ path('library_update_process') }}">
    {% for message in app.flashes('notice') %}
        <p>
            {{ message }}
        </p>
    {% endfor %}
            <p><input type="text" name="title" required placeholder="Titel" value="{{ book.title }}"></p>
            <p><input type="text" name="isbn" required placeholder="ISBN" value="{{ book.isbn }}"></p>
            <p><input type="text" name="author" required placeholder="Författare" value="{{ book.author }}"></p>
            <p><input type="text" name="img" required placeholder="Bild" value="{{ book.img }}"></p>
            <p><button type="submit" name="submit" value="{{ book.id }}">Uppdatera bok</button></p>
    </form>
{% endblock %}
